import PasswordInputPage from "@/components/passwordInput";
import react from "react";

export default function Paren() {
    return (
        <PasswordInputPage codeBlock=")" blockKey="paren" hint="UCI's school of business' namesake invented this delectable pastry feast. (answer in all lower case)" />
    )
}